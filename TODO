(A) make it so datasets get pulled automaticly and python scripts generate new proccesed data
(B) Minimum number of days since outbreak from all states does not need to be used anymore. gen1-cleaned fixed the issue that prevented this
(C) Create new data generation model that uses all countries in order to have more samples
(D) Clean up mess of plotting functions and eleminate duplication
